vec4 halfVec(vec4 a)
{
  return 0.5 * a;
}

kernel vec4 kern(sampler s, __color foo, vec4 parm3)
{
  vec2 p = exp(vec2(2,2));
  vec2 dc = destCoord();
  float a = compare(-1.0, 7.0, 5.0);
  vec2 sc = cossin_(4.0);
  vec4 pm = premultiply(foo);
  //vec2 sz = samplerSize(s);
  for(int i=0; i<4; i++)
  {
    a += 1.0;
  }
  vec4 g = sample(s, vec2(1,1)) * foo;
  return vec4(1,1,1,1);
}
