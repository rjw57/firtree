// Trivial function test

uniform float input;

float half(float a)
{
    return 0.5*a;
}

vec4 offset_sin(vec4 a)
{
    return 0.5 + 0.5 * sin(a);
}

kernel void testKernel(vec4 param)
{
    float a = half(param.y);
    float b = half(0.5);
    vec4 c = offset_sin(vec4(a,a,0,0));
    float d = destCoord();

    //return c;
}

// vim:sw=4:ts=4:et:cindent:filetype=c
