vec4 noparamFunc()
{
  return vec4(5,6,7,8);
}

void pointlessFunc(int a)
{
  int b = a++;
}

vec4 halfVec(vec4 foo, float fac)
{
  return fac * foo;
}

kernel vec4 kern(sampler s, __color foo, vec4 parm3)
{
  float f = 1.0;
  float g = f * 6.0 + 1.0;
  vec4 j;
  vec2 p = exp(vec2(2,2));
  vec4 test = halfVec(vec4(6,7,8,9), 0.5);
  vec2 dc = destCoord();
  j = vec4(1,2,3,4);

  {
    vec4 j;
    j = vec4(6,7,8,9);
  }
  float a = compare(-1.0, 7.0, 5.0);
  vec2 sc = cossin_(4.0);
  vec4 pm = premultiply(foo);
  //vec2 sz = samplerSize(s);
  /*
  for(int i=0; i<4; i++)
  {
    a += 1.0;
  }
  */
  a += p.x;
  vec4 og = sample(s, vec2(1,1)) * foo;
  return noparamFunc();
}
